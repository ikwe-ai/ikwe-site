!function() {
    var e, t, n, i = window.location, o = window.document, r = {};
    function a(e, t, n) {
        window.fetch && fetch(e, {
            method: 'POST',
            headers: {
                'Content-Type': 'text/plain'
            },
            keepalive: !0,
            body: JSON.stringify(t)
        }).then(function(e) {
            n && n.callback && n.callback({
                status: e.status
            });
        }).catch(function() {});
    }
    var c = !1, l = location.href, s = {}, u = -1, d = 0, p = 0;
    function f() {
        var i = v();
        if (!e && (u < n || i >= 3000)) {
            u = n;
            var o = {
                n: 'engagement',
                sd: Math.round(n / t * 100),
                d: r.domain,
                u: l,
                p: s,
                e: i,
                v: 14
            };
            d = 0, p = 0, r.hashBasedRouting && (o.h = 1), a(r.endpoint, o);
        }
    }
    function m() {
        'visible' === document.visibilityState && document.hasFocus() && 0 === d ? d = Date.now() : 'hidden' !== document.visibilityState && document.hasFocus() || (p = v(), d = 0, f());
    }
    function v() {
        return d ? p + (Date.now() - d) : p;
    }
    function track(v, w) {
        if (!r.isInitialized) throw Error('plausible.track() can only be called after plausible.init()');
        var g, b = 'pageview' === v;
        if (b && c && (f(), k = document.body || {}, S = document.documentElement || {}, t = Math.max(k.scrollHeight || 0, k.offsetHeight || 0, k.clientHeight || 0, S.scrollHeight || 0, S.offsetHeight || 0, S.clientHeight || 0), x = document.body || {}, H = document.documentElement || {}, E = window.innerHeight || H.clientHeight || 0, _ = window.scrollY || H.scrollTop || x.scrollTop || 0, n = t <= E ? t : _ + E), !r.captureOnLocalhost) {
            if (/^localhost$|^127(\.[0-9]+){0,2}\.[0-9]+$|^\[::1?\]$/.test(i.hostname) || 'file:' === i.protocol) return h(v, w, 'localhost');
            if ((window._phantom || window.__nightmare || window.navigator.webdriver || window.Cypress) && !window.__plausible) return h(v, w);
        }
        try {
            if ('true' === window.localStorage.plausible_ignore) return h(v, w, 'localStorage flag');
        } catch (e) {}
        var y = {};
        if (y.n = v, y.v = 14, y.u = w && w.u || i.href, y.d = r.domain, y.r = o.referrer || null, w && w.meta && (y.m = JSON.stringify(w.meta)), w && w.props && (y.p = w.props), w && !1 === w.interactive && (y.i = !1), w && w.revenue && (y.$ = w.revenue), r.customProperties) {
            var k, S, x, H, E, _, L, g = r.customProperties;
            'function' == typeof g && (g = r.customProperties(v)), (void 0 === g ? "undefined" : (L = g) && "undefined" != typeof Symbol && L.constructor === Symbol ? "symbol" : typeof L) == 'object' && (y.p = Object.assign({}, g, y.p));
        }
        r.hashBasedRouting && (y.h = 1), b && (e = !1, l = y.u, s = y.p, u = -1, p = 0, d = Date.now(), c || (document.addEventListener('visibilitychange', m), window.addEventListener('blur', m), window.addEventListener('focus', m), c = !0)), a(r.endpoint, y, w);
    }
    function h(t, n, i) {
        i && console.warn('Ignoring Event: ' + i), n && n.callback && n.callback(), 'pageview' === t && (e = !0);
    }
    var w = [
        'pdf',
        'xlsx',
        'docx',
        'txt',
        'rtf',
        'csv',
        'exe',
        'key',
        'pps',
        'ppt',
        'pptx',
        '7z',
        'pkg',
        'rar',
        'gz',
        'zip',
        'avi',
        'mov',
        'mp4',
        'mpeg',
        'wmv',
        'midi',
        'mp3',
        'wav',
        'wma',
        'dmg'
    ];
    function g(e, t, n) {
        var i = !1;
        function o() {
            i || (i = !0, window.location = t.href);
        }
        if (function(e, t) {
            if (e.defaultPrevented) return !1;
            var n = !t.target || t.target.match(/^_(self|parent|top)$/i), i = !(e.ctrlKey || e.metaKey || e.shiftKey) && 'click' === e.type;
            return n && i;
        }(e, t)) {
            var r = {
                props: n.props,
                callback: o
            };
            r.revenue = n.revenue, track(n.name, r), setTimeout(o, 5000), e.preventDefault();
        } else {
            var r = {
                props: n.props
            };
            r.revenue = n.revenue, track(n.name, r);
        }
    }
    function b(e) {
        var t = e && e.classList;
        if (t) {
            for(var n = 0; n < t.length; n++)if (t.item(n).match(/plausible-event-name(=|--)(.+)/)) return !0;
        }
        return !1;
    }
}();
